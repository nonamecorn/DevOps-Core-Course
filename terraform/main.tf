terraform {
  required_version = ">= 1.9.0"
}

# Local VM alternative:
# This repository uses WSL as the lab host instead of a cloud VM.
# Cloud provider resources are intentionally not declared here.
